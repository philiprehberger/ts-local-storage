# Changelog

## 0.1.0 (2026-03-21)

- Initial release
- `createStore()` for type-safe storage access
- Typed `get` / `set` with JSON serialization
- TTL support for automatic expiry
- Key prefixing for namespace scoping
- SSR-safe with in-memory fallback
